// Placeholder set of selectable gift-card backgrounds for the canvas editor (Phase 3).
// Populated later with real background assets. Shape (future): { id, name, src }.
export const giftBackgrounds = []
