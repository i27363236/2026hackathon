// Placeholder set of MRT station photos the user can add in the canvas editor (Phase 3).
// Populated later with real station images. Shape (future): { id, name, src }.
export const stationPhotos = []
